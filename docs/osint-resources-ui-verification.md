# OSINT Resources — UI Verification

## Desktop

At `1440×1100`, the new OSINT Resources route keeps the title, explicit exclusion boundary, and local research protocol in the editorial left column. The reviewed-source entries render as ledger-style records with issuer labels, use boundaries, and user-controlled external links. The active sidebar destination is clearly visible.

## Mobile

At `390×844`, the exclusion notice, protocol, filter controls, each source record, and the Ontario-first route cards stack without clipped text. The menu trigger remains visible, and the page does not expose any excluded phone, tracking, credential, scanning, or exploit tools.
